{
  "name": "insurance-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "CoverLens: Singapore insurance policy checker (apps/insure) built on the platform template's Next.js + AWS serverless stack.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:spec-test": "npm run build --workspace packages/spec-test",
    "typecheck": "npm run build:spec-test && npm run typecheck --workspaces --if-present",
    "lint": "npm run build:spec-test && npm run lint --workspaces --if-present",
    "test": "npm run build:spec-test && npm run test:unit --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@commitlint/types": "^19.0.0",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.60.0"
  }
}
